# Frontend Configuration
# API URL is automatically set based on environment:
# - Development (npm run dev): http://localhost:3000/api
# - Production (npm run build): /api (relative path, works with reverse proxy)
# No need to set API_URL manually
