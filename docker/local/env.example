# Copy to .env in this directory:  cp env.example .env
# docker compose reads .env next to docker-compose.yml for variable substitution.

# Host bind: 127.0.0.1 (default) or 0.0.0.0 for all interfaces
POSTGRES_BIND=127.0.0.1
POSTGRES_HOST_PORT=5432
POSTGRES_USER=crypto
POSTGRES_PASSWORD=crypto_local_dev
POSTGRES_DB=chuyen_gia
