// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/chuyen-gia-crypto/backend/internal/db/ent/latestprice"
)

// LatestPrice is the model entity for the LatestPrice schema.
type LatestPrice struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Coin holds the value of the "coin" field.
	Coin string `json:"coin,omitempty"`
	// Price holds the value of the "price" field.
	Price float64 `json:"price,omitempty"`
	// Change24h holds the value of the "change_24h" field.
	Change24h float64 `json:"change_24h,omitempty"`
	// Change7d holds the value of the "change_7d" field.
	Change7d float64 `json:"change_7d,omitempty"`
	// MarketCap holds the value of the "market_cap" field.
	MarketCap float64 `json:"market_cap,omitempty"`
	// Volume24h holds the value of the "volume_24h" field.
	Volume24h float64 `json:"volume_24h,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LatestPrice) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case latestprice.FieldPrice, latestprice.FieldChange24h, latestprice.FieldChange7d, latestprice.FieldMarketCap, latestprice.FieldVolume24h:
			values[i] = new(sql.NullFloat64)
		case latestprice.FieldID:
			values[i] = new(sql.NullInt64)
		case latestprice.FieldCoin:
			values[i] = new(sql.NullString)
		case latestprice.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LatestPrice fields.
func (_m *LatestPrice) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case latestprice.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case latestprice.FieldCoin:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field coin", values[i])
			} else if value.Valid {
				_m.Coin = value.String
			}
		case latestprice.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				_m.Price = value.Float64
			}
		case latestprice.FieldChange24h:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field change_24h", values[i])
			} else if value.Valid {
				_m.Change24h = value.Float64
			}
		case latestprice.FieldChange7d:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field change_7d", values[i])
			} else if value.Valid {
				_m.Change7d = value.Float64
			}
		case latestprice.FieldMarketCap:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field market_cap", values[i])
			} else if value.Valid {
				_m.MarketCap = value.Float64
			}
		case latestprice.FieldVolume24h:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field volume_24h", values[i])
			} else if value.Valid {
				_m.Volume24h = value.Float64
			}
		case latestprice.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LatestPrice.
// This includes values selected through modifiers, order, etc.
func (_m *LatestPrice) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this LatestPrice.
// Note that you need to call LatestPrice.Unwrap() before calling this method if this LatestPrice
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *LatestPrice) Update() *LatestPriceUpdateOne {
	return NewLatestPriceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the LatestPrice entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *LatestPrice) Unwrap() *LatestPrice {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: LatestPrice is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *LatestPrice) String() string {
	var builder strings.Builder
	builder.WriteString("LatestPrice(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("coin=")
	builder.WriteString(_m.Coin)
	builder.WriteString(", ")
	builder.WriteString("price=")
	builder.WriteString(fmt.Sprintf("%v", _m.Price))
	builder.WriteString(", ")
	builder.WriteString("change_24h=")
	builder.WriteString(fmt.Sprintf("%v", _m.Change24h))
	builder.WriteString(", ")
	builder.WriteString("change_7d=")
	builder.WriteString(fmt.Sprintf("%v", _m.Change7d))
	builder.WriteString(", ")
	builder.WriteString("market_cap=")
	builder.WriteString(fmt.Sprintf("%v", _m.MarketCap))
	builder.WriteString(", ")
	builder.WriteString("volume_24h=")
	builder.WriteString(fmt.Sprintf("%v", _m.Volume24h))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// LatestPrices is a parsable slice of LatestPrice.
type LatestPrices []*LatestPrice
